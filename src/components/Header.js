import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                my header
            </header>
        )
    }
}

export default Header;